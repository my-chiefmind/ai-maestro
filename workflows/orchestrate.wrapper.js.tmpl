/**
 * {{PROJECT_NAME}} — Orchestrate Workflow
 * Generated by AI Maestro v{{KIT_VERSION}}
 *
 * DO NOT EDIT this file directly. It is generated by render/sync.mjs (opt-in via
 * config.targets.workflow). Edit config.json / context.md to change project-specific
 * behavior, then re-run sync.
 *
 * Run it with the Workflow tool: Workflow({ name: "orchestrate" }) — or with args:
 * "start <id>" | "status <id>" | "resume <id>" | "abort <id>"; empty picks the next
 * unblocked TODO ticket.
 */

// ── Project Configuration (injected by sync.mjs) ──────────────────────────────

const PROJECT_CONFIG = {
  PROJECT_ROOT: {{PROJECT_ROOT_JSON}},
  BOARD:        {{BOARD_ABS_JSON}},
  ARCHIVE:      {{ARCHIVE_ABS_JSON}},
  WORKTREES:    {{WORKTREES_JSON}},
  RUNS:         {{RUNS_JSON}},
  VALIDATE_CMD: {{VALIDATE_CMD_JSON}},

  // "pr" = push branch + PR + squash-merge via gh (protected mains);
  // "local-push" = local --no-ff merge, then push main. Never local-only.
  MERGE_STRATEGY: {{MERGE_STRATEGY_JSON}},
  // Commit + push board transitions (true) or keep board writes local-only (false).
  PUBLISH_BOARD: {{PUBLISH_BOARD_JSON}},

  REPO_PATH:  {{REPO_PATH_JSON}},
  TEST_CMD:   {{TEST_CMD_JSON}},
  AGENT_TYPE: {{AGENT_TYPE_JSON}},
  AGENTS:     {{AGENTS_JSON}},
  AREA_PLAN:  {{AREA_PLAN_JSON}},

  // File-path patterns routing fix agents after a qa BLOCK — { agentCode: regexString },
  // matched against filesChanged[] and findings[].
  FIX_AGENT_HINTS: {{FIX_AGENT_HINTS_JSON}},
};

// ── Shared Core (embedded by sync.mjs from workflows/orchestrator-core.js) ────
{{ORCHESTRATOR_CORE}}
