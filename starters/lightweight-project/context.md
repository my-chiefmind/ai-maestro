# my-lib — project context

_Every agent reads this. Keep it short and accurate._

## What this is

A library / app that wants Maestro's agents and skills without an active board.

## Stack & conventions

- Language / toolchain and how to build.
- Test command: `<command>`.
- Match existing code style.

## Guardrails

- `main` is protected — branch + PR.
- Secrets are never committed.
