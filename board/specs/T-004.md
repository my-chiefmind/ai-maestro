# T-004 — toolkit/ vs @mychiefmind/ai-maestro diff (upstream worklist + deletion evidence)

Snapshot 2026-08-08, against ai-maestro v0.1.21 (`~/source/ai-maestro`, read-only survey).
Every file in `toolkit/` (60 files) classified: **upstream** (generic → ai-maestro PR),
**project-specific** (→ that product's `maestro/context.md`), or **dead** (delete with no
migration). This file is the deletion evidence kit-091 requires.

Filed on lense-kit as `kit-081` and moved onto this board 2026-08-08. The remaining work is
entirely in this repo, which is where the ticket now lives.

## Progress (2026-08-08)

**Wave 1 — missing-entirely items, MERGED** (owner merged same day; v0.1.22 released, owner
follow-up aliased pipeline-developer → pipeline):

- **PR #12** — `frontend-quality` skill (budgets moved to a `context.md` declaration).
- **PR #13** — `pipeline-developer` agent (code: `pipeline`) + `pipeline-quality` skill.
- **PR #14** — `orchestration-health` skill (rewritten for the package runtime model).

**Wave 2 — §2 drift ports, MERGED 2026-08-08** (merge commits 4081a5e…fb09aae, branches
deleted; combined main suite 52/52 green — not yet released to npm):

- **PR #15** — qa: adversarial checklist (test anti-patterns, diff hygiene, per-AC evidence
  table, "no ACs = block").
- **PR #16** — orchestrator: reconcile-before-claiming, capped fix loops, protected-main PR
  strategy, abort mode.
- **PR #17** — security-review: rotation-not-redaction, SSRF specifics, rate limits, real
  audit tooling.
- **PR #18** — release-gate: fail closed (zero-AC no-go, verdict verification, mergeability,
  structured GO/NO-GO output).
- **PR #19** — roster hard rules: backend, frontend, devops, pd, pe (one commit).
- **PR #20** — validator/schema: deps resolve against `archive.json` (hard error), archive
  validated + cross-file id uniqueness, archive-only terminal states, `failureKind`,
  human-gate eligibility warning; +9 tests (`test/validate-archive.test.mjs`).

**Remaining**: §1 tail (templates + `orchestrator-core.js` — needs an upstream design call on
a JS harness / multi-target render), §2 item 12 (`render/sync.mjs` feature ports —
deliberately last), §3 overlay relocations to agrolense/speclense.

## 1. UPSTREAM — missing from the package entirely

| Item | Notes |
|---|---|
| `toolkit/skills/frontend-quality/SKILL.md` | **PR #12, merged** |
| `toolkit/skills/orchestration-health/SKILL.md` | **PR #14, merged** |
| `toolkit/skills/pipeline-quality/SKILL.md` | **PR #13, merged**; project deltas live in overlays (§3) |
| `toolkit/agents/_base/pipeline-developer.md` | **PR #13, merged** |
| `toolkit/workflows/orchestrator-core.js` | package has only `skills/orchestrator/SKILL.md`; the JS harness is missing |
| `toolkit/schemas/handoff.schema.json` | belongs with orchestrator-core |
| `toolkit/schemas/merge-response.schema.json` | belongs with orchestrator-core |
| `toolkit/schemas/run-record.schema.json` | belongs with orchestrator-core |
| `toolkit/templates/AGENTS.md.tmpl` | package render emits only CLAUDE.md; AGENTS.md not covered |
| `toolkit/templates/codex-agent.toml.tmpl` | not covered by package render |
| `toolkit/templates/orchestrate.wrapper.js.tmpl` | not covered; pairs with orchestrator-core |
| kit-075 §2 gap fixes (2a silent config degrade, 2b agent-code aliasing, 2c `--yes`, 2d `setup` cwd) | behavioral fixes, already itemized in `board/specs/kit-075.md` |

## 2. UPSTREAM — exists in package but DRIFTED (diff-and-merge, keep the better lines)

Umbrella copies have evolved past (or apart from) the package. None are identical.

### §2 triage (2026-08-08, read-only pass over all 13 pairs)

12 of 13 pairs carried umbrella capability the package lacked; only `delivery-hygiene` is
pure deliberate divergence (nothing to port). The port-worthy items became PRs #15–#20 (see
Progress above); the only §2 item still unported is **12: `render/sync.mjs` features** —
roster-typo warning (package silently drops unknown roster entries), config-driven opt-in
gates rendered ENABLED/SKIPPED, kit digest + config hash in the lock (distinct staleness
causes), `--all` batch mode with per-project error isolation, AGENTS.md output, multi-target
(Codex) rendering, generic `{{KEY}}` templating with filters.

**Do not lose in any renderer merge** (package-side safety the umbrella lacks): prune only
what the prior lock generated, never overwrite a CLAUDE.md it didn't create, project
overlays survive re-render.

- All 7 core agents (`toolkit/agents/_core/*.md` vs package `agents/*.md`): backend-developer
  (182 diff lines), qa (177), orchestrator (162), frontend-developer (156),
  principal-engineer (148), devops (147), principal-delivery (133).
- 4 shared skills: security-review (109), board-validate (105), release-gate (97),
  delivery-hygiene (65).
- `toolkit/render/sync.mjs` vs package `render/sync.mjs` — 692 diff lines; largest merge job.
- `toolkit/schemas/board.schema.json` vs package `board/board.schema.json` — 41 diff lines.
- `toolkit/schemas/lense-config.schema.json` vs package `starters/*/config.json` shape —
  package validates config ad hoc; decide schema-vs-inline upstream.
- `toolkit/schemas/lock.schema.json` — lock handling is inlined in package render; upstream
  the schema or confirm inline coverage.

## 3. PROJECT-SPECIFIC — goes to product repos, NOT ai-maestro

- `toolkit/projects/agrolense/pipeline-quality.overlay.md` → `~/source/agrolense` maestro context
- `toolkit/projects/speclense/pipeline-quality.overlay.md` → `~/source/speclense` maestro context

## 4. DEAD — delete with toolkit/, nothing to migrate

- `toolkit/starters/**` (14 files) — package ships its own `starters/` (config.json/context.md
  format); the lense.config.json-format starters are the retired contract.
- `toolkit/templates/CLAUDE.md.tmpl`, `toolkit/templates/lense-bootstrap.mjs.tmpl` — covered
  by package render / obsolete bootstrap path.
- `toolkit/scripts/*` (8) — validate-all, check-contracts, check-artifacts, docs-portal-check,
  export-project, inventory, migrate-board, open-board: all scan for `*-group/` projects,
  zero remain post-kit-e15. Package has validate-board/board-core/cockpit-install.
- `toolkit/tests/*` (4 contract tests) — test the retired contract; package has its own `test/`.
- `toolkit/VERSION`, `toolkit/CLEANUP-INITIATIVE-CLOSE.md`.
- `toolkit/KEEP-LIST.md` — content still matters (PII/CRM + SpecLense pipeline protections);
  relocate the list into `docs/` or the affected product repos before deleting the file.

## 5. Repo unwiring required at deletion time (kit-091)

Root `package.json` scripts all point into toolkit/ and break on delete:
`board` (open-board.mjs), `test` (toolkit/tests), `check` (validate-all.mjs),
`check:ci` (check-artifacts + render --check). Replace: `board` → open cockpit directly;
`check`/`test` → package equivalents or retire; also `.githooks/pre-commit` (runs `npm run check`)
and the CLAUDE.md Commands/Architecture sections describing toolkit.

## 6. Tickets/data — what does NOT move

**Amended 2026-08-08 (owner):** this section previously asserted that no lense-kit board ticket
migrates to ai-maestro's own board. That is reversed — package work belongs on the package's
board, and T-001/T-002/T-003/T-004 moved here from `kit-096/082/097/081`.

What stays on lense-kit's board: `kit-091` (delete `toolkit/` from that repo), `kit-099` (delete
`cockpit/` from that repo), `kit-098` (the ledger move, done), and `kit-075` (dogfooding the
package *there*). Those are all lense-kit changes that merely depend on work here.

`projects.json` and the umbrella board stay in lense-kit — portfolio-scoped by `kit-091`.
