# Node
node_modules/
npm-debug.log*
.npm/

# Maestro runtime (generated per-project; never committed from the kit)
.maestro/run/
.maestro/worktrees/
*.lock.local

# Board write backups (the web dashboard keeps timestamped copies; they're local-only)
.backups/

# Private dev repo: the live workboard (data, archive, plan, specs, reports) IS tracked here.
# The public repo is a clean snapshot and never includes it.

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code: track agent memory/config in this private repo; never its worktrees
.claude/worktrees/
.claude/**/.DS_Store
